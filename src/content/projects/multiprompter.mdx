---
name: Multiprompter
tagline: a mac teleprompter where one script drives many scroll-synced windows
description: |
  a native mac teleprompter built in swift. one markdown script drives any
  number of frameless viewer windows, all kept in precise scroll sync, so a
  big talking-head prompt and a thin strip floating over your code stay on the
  exact same line.
status: live
badge: mac app
url: https://multiprompter.app
repoUrl: https://github.com/multiprompter/releases
stack: [Swift, SwiftUI, AppKit, macOS, Astro, Lemon Squeezy]
heroImage: ./multiprompter-website.webp
heroImageAlt: Multiprompter homepage with the headline "Your script, everywhere you look", pricing copy, a download button, and two product mockups showing synced teleprompter windows.
publishDate: 2026-06-20
updatedDate: 2026-08-20
featured: false
---

## what it does

write your script once, run it across as many windows as you want:

- **one script, n windows**: a markdown editor with live preview drives any
  number of frameless viewer windows
- **precise cross-window scroll sync**: every window stays on the same spot,
  even at different font sizes, because sync is by logical position (a source
  line plus how far you are through that paragraph), not pixel scroll
- **per-viewer font, top-anchored**: each window sizes its own text; they match
  on the top line, which is what actually helps when your screens aren't
  physically centered
- **capture-safe viewer windows**: the floating prompt can sit over the screen
  you record without appearing in that recording
- **persistence**: viewers and the editor frame reopen exactly where you left
  them
- **trial and license flow**: the app has a 14-day trial and a one-time license
  model through its own site
- **public release path**: the current notarized DMG lives in a public releases
  repo, with a product-owned Homebrew tap for terminal installs

## why i built it

i record a lot of tutorials and talking-head videos, and i wanted a teleprompter
that could run more than one window at once: a large prompter for my face cam and
a thin strip sitting over my editor, both scrolling together. every teleprompter
i found is single-window. it's also the standalone, multi-window cousin of
[linesmith](/tools/linesmith), my vs code teleprompter.

the other reason: it's my first swift app. i wanted to learn the language by
building something real i'd actually use, not a toy.

## how it's built

a quick tour:

- **swift + swiftui + appkit**: swiftui for the shell, appkit where it counts
- **one shared scroll engine**: the editor, the preview, and every viewer scroll
  through the same `NSTextView`-based line-mapping engine, after swiftui's
  `scrollPosition` proved too coarse and a beat behind
- **fractional sync position**: the sync unit is `line + fraction through the
  paragraph`, so long wrapped paragraphs can't let two windows drift apart
- **echo suppression for "who's driving"**: any window broadcasts as it scrolls
  and ignores the one scroll it caused by following, which beat the scroll-phase
  and hover approaches that kept getting stuck
- **borderless AppKit windows**: viewers are frameless `NSWindow`s with the
  capture-exclusion, always-on-top, opacity, and click-through behavior handled
  at the window layer
- **mac distribution**: Developer ID signing, notarization, a DMG release, and a
  Homebrew cask path for installing from the terminal
- **public site**: [multiprompter.app](https://multiprompter.app) is an Astro
  site with docs, changelog, support, and a product scene based on the real app
  UI

## what's next

- **auto-scroll**: the obvious next prompting control now that the core sync
  and product surface are real
- **more release hardening**: the public download, license flow, support path,
  and cask should stay boring and repeatable
- **phase 2: voice tracking**: your voice drives the same scroll position the
  windows already share

> the windows sharing one position is the whole app. everything after this,
> voice tracking included, is just another thing that moves that position.
